# AI Opener Configuration
# Copy this file to .env and customize

# Port to listen on (default: 3000)
PORT=3000

# Target backend host to forward requests to
TARGET_HOST=localhost

# Target backend port to forward requests to
TARGET_PORT=8080
